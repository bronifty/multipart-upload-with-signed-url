import React from "react";
import { HomeButton, PageContainer } from "../components";
const NotFound = (props) => (<PageContainer header={<HomeButton />}>404 Page Not Found</PageContainer>);
export default NotFound;
